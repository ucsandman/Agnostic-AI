# Agnostic AI Harness — Python runtime dependencies.
# Kept in sync with setup.py's install_requires; a fresh `pip install -e .`
# must be able to import agent.tui and agent.cli using only these.
openai>=1.0.0,<3.0.0
rich>=13.0.0,<16.0.0
httpx>=0.25.0,<1.0.0
prompt_toolkit>=3.0.0,<4.0.0
textual>=0.40.0,<9.0.0
questionary>=2.0.0,<3.0.0
pillow>=10.0.0,<13.0.0

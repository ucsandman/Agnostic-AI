---
name: Bug report
about: Something does not work as documented
labels: bug
---

**What happened**

**What you expected**

**Steps to reproduce**

1.
2.

**Environment**

- OS:
- Python version (`python --version`):
- Node version (`node --version`):
- Entry point: `agnostic` (TUI) / `agnostic-legacy` / `npm run dashboard` / other:
- Model endpoint: LM Studio / Ollama / hosted API / subscription CLI:
- Harness version (`agnostic --version`):

**Logs**

Paste relevant output. Redact anything sensitive.

# Running prototype shortlist (Phase 17 picks THREE)

Each entry: capability it depends on · confirmed? · smallest validating experiment · expected value · competes for a build slot?

| # | Concept | Depends on | Confirmed | Smallest experiment | Expected value | Slot? |
|---|---|---|---|---|---|---|
| S1 | **Runtime adapter** (`ClaudeRuntimeEvent` bus + `supports.*` negotiation + `judge()` interface) | `on("*")`, `next.event/origin/trace`, `agent.spawn`, `turn.*`, `$.session.usage()` | YES (all RUN) | one plugin that emits normalised events to a JSONL + exposes `supports` from live probing; two consumers (a HUD, a recorder) read it | every other prototype sits on it; isolates the early-access API in one file | **YES** |
| S2 | **Router + budget Mod** (replaces 4 harness guards; declared→measured; deny→rewrite) | `agent.spawn` rewrite, per-agent `turn.complete.usage`, `$.store` | YES (spawn rewrite RUN, usage RUN) | spawn a haiku-scout, rewrite model, record cost, refit constants into `$.store`; show `routing_log` in a pane | largest single harness win; deletes retry pathology | **YES** |
| S3 | **CostClaw LIVE** (thrash interceptor, cache-decay autopilot, model-misroute prior, live HUD) | `turn.step` usage, `tool.call` result replacement, `$.session.usage()` | YES | port `pricing.ts` + `rebuildSessionUsage` into a Mod; answer the 11th identical Read from cache; HUD | turns a report into prevention; connects to costclaw.io product | **YES** |
| S4 | **DashClaw native adapter** (`classifyAct` + 15 pure evaluators in-process; `tool.check` approval seam) | `tool.call` deny, `tool.check`, `$.clock.every`, `$.prompt.submit` | YES for deny/check; approval-park path DECL | port `evidence.ts` + `claude-code-starter` pack; deny a `git push --force`; park an approval and wake via `$.prompt.submit` | removes 120 KB Python + 3,660 s timeout; policy core untouched | **YES** |
| S5 | **Safe-boundary handoff + flight recorder** (Leg tap + bundle from events) | `turn.complete`, `$.turn.abort` (DECL), `tool.call`, `$.model.complete` | partial (`turn.abort` not exercised) | at ≥N % usage set a flag; at `turn.complete` write a bundle from the event ledger and abort; verify Leg's `handoff.mjs` accepts it | fixes mid-edit kills; makes handoffs automatic | YES |
| S6 | **GUARDIAN-grade production awareness** (offlocal `evaluatePolicy` × `classifyAct` on Bash) | `tool.call`, `prompt.submit` hidden context, `ui.render` footer | YES | classify `vercel deploy --prod` → `deploy` × `production` → `ask`; footer shows project/env | closes offlocal's blind spot with ~200 lines | maybe (folds into S4) |
| S7 | **giti-at-the-edit** (fragility/coupling as hidden result context) | `tool.call` context, `$.process.run("git log")` | YES | one session on a repo with history; edit a hot file; see the coupling note | cheap, vivid, new capability | maybe |
| S8 | **Memory candidate feed** (RAM tier only, `[observed]`, promote via consent token in a pane) | `tool.call`, `prompt.submit origin`, `ui.render` Pane + Button | YES (pane needs ≥144 cols) | write 3 candidates to `memory/YYYY-MM-DD.md`, pane with promote button gated by `confirm.ts` port | keeps policy; adds the missing signal and click | maybe |
| S9 | **Session replay (agent-pit format)** record → play in a pane; replay-against-recorded-results | `on("*")`, `tool.call` answer-without-next, `ui.render` | YES | record demo session into `EventStreamReplay`; replay a Read from the record without touching disk | tangible "git for execution" lite | maybe |
| S10 | **declick enforcement arm** (`capData` on every result; answer `mcp__*` via `declick run`) | `tool.call` result replacement, `$.process.run` | YES | one MCP call answered from declick; one 20 KB Read capped to 8 KB with `meta.capped` | fixes a measured 5.6 % follow rate | maybe |
| S11 | **Jev judgment layer** behind `runtime.judge()` (Jev / haiku / stub backends) | none from Mods; needs `TYPESAFE_API_KEY` | key MISSING on this machine | run `experiments/jev/run_judge.py jev` once the key lands (≤60k tokens) | validates whether typed judgments beat rules on ambiguous states | telemetry only |
| S12 | **Discovery Loop observation feed** | `on("*")` + a writer into `research_memory` shape | YES (observations only) | write 5 candidate-intervention rows from a session; run `summarize_development` | honest: observations, never confirmations | no (fold into S1's recorder) |

Status column reflects `MOD_CAPABILITY_MAP.md` §20 as of 2026-09-16.

**Phase 17 selection (20:30):** S1 (adapter) built and proven; the three build slots went to **S2 supervisor**, **S3 costclaw-live**, **S4+S6 prodguard** (offlocal folded in), chosen for: all-CONFIRMED dependencies, a visible on-screen behaviour, direct product lineage (harness/LegCli, CostClaw, DashClaw/offlocal), and safety (subagent-model rewrites, cache serving, and pre-execution denies are all reversible and local). S5 became `lab/spike-handoff` (abort + bundle proven), S8's compaction half became `lab/spike-compact` (proven), S10's `tool.describe`/`agent.offer` dependencies were proven by `lab/spike-describe`. S7, S8 (pane), S9, S10, S12 remain on the list for the roadmap.
